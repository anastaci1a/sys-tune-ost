diff --git a/Makefile b/Makefile
index 7e82c79..d28ffc8 100644
--- a/Makefile
+++ b/Makefile
@@ -1,5 +1,5 @@
 export GITHASH 		:= $(shell git rev-parse --short HEAD)
-export VERSION 		:= 2.4.0-exp.qs7
+export VERSION 		:= 2.4.0-exp.qs8
 export API_VERSION 	:= 15
 export WANT_FLAC 	:= 1
 export WANT_MP3 	:= 1
diff --git a/common/config/config.cpp b/common/config/config.cpp
index cb95606..619ae60 100644
--- a/common/config/config.cpp
+++ b/common/config/config.cpp
@@ -385,6 +385,18 @@ void set_quick_settings_volume(float value) {
         std::clamp(value, 0.f, 1.f), CONFIG_PATH);
 }
 
+auto get_loading_start_delay_ms() -> u32 {
+    return std::clamp<long>(ini_getl(
+        "ost_manager", "loading_start_delay_ms", 500, CONFIG_PATH), 0, 5000);
+}
+
+void set_loading_start_delay_ms(u32 value) {
+    create_config_dir();
+    ini_putl(
+        "ost_manager", "loading_start_delay_ms",
+        std::min(value, 5000u), CONFIG_PATH);
+}
+
 auto get_fade_in_ms() -> u32 {
     return std::clamp<long>(ini_getl("ost_manager", "fade_in_ms", 150, CONFIG_PATH), 0, 5000);
 }
diff --git a/common/config/config.hpp b/common/config/config.hpp
index 40480ae..c8e87b1 100644
--- a/common/config/config.hpp
+++ b/common/config/config.hpp
@@ -79,6 +79,10 @@ void set_album_video_volume(float value);
 auto get_quick_settings_volume() -> float;
 void set_quick_settings_volume(float value);
 
+// Time between detecting an application launch and starting Loading OST.
+auto get_loading_start_delay_ms() -> u32;
+void set_loading_start_delay_ms(u32 value);
+
 auto get_fade_in_ms() -> u32;
 void set_fade_in_ms(u32 value);
 auto get_fade_out_ms() -> u32;
diff --git a/overlay/source/gui_misc.cpp b/overlay/source/gui_misc.cpp
index 018c484..1fad750 100644
--- a/overlay/source/gui_misc.cpp
+++ b/overlay/source/gui_misc.cpp
@@ -88,6 +88,14 @@ tsl::elm::Element* MiscGui::createUI() {
         },
         1.f));
 
+    list->addItem(new tsl::elm::CategoryHeader("Loading Screen"));
+    list->addItem(new ElmTextBlock(
+        "Waits after launch detection before\n"
+        "starting Loading Screen OST."));
+    list->addItem(MakeDurationSlider(
+        "Start Delay", config::get_loading_start_delay_ms(),
+        config::set_loading_start_delay_ms));
+
     list->addItem(new tsl::elm::CategoryHeader("Track Boundaries"));
     list->addItem(new ElmTextBlock(
         "Used when tracks start or end naturally.\n"
diff --git a/sys-tune/source/impl/music_player.cpp b/sys-tune/source/impl/music_player.cpp
index 6200065..6b59033 100644
--- a/sys-tune/source/impl/music_player.cpp
+++ b/sys-tune/source/impl/music_player.cpp
@@ -248,6 +248,7 @@ std::atomic<u32> g_fade_in_ms = 150;
 std::atomic<u32> g_fade_out_ms = 0;
 std::atomic<u32> g_mid_song_fade_in_ms = 300;
 std::atomic<u32> g_mid_song_fade_out_ms = 500;
+std::atomic<u32> g_loading_start_delay_ms = 500;
 
 std::atomic<float> g_global_volume = 1.f;
 std::atomic<float> g_state_volume = 1.f;
@@ -1195,6 +1196,11 @@ void PmdmntThreadFunc(void*) {
     bool gated_home_seen = false;
     u64 gated_home_since_tick = 0;
     u32 last_coordinated_transition = 0;
+    bool loading_delay_active = false;
+    u64 loading_delay_since_tick = 0;
+    u64 previous_process_target = applet_bgm::SilentTitleId;
+    u32 applet_home_scene_baseline = 0;
+    bool applet_home_override = false;
 
     while (g_should_run) {
         const bool reload = g_applet_bgm_reload.exchange(false);
@@ -1241,6 +1247,36 @@ void PmdmntThreadFunc(void*) {
         }
         const auto scene_snapshot = qlaunch_scene::GetSceneSnapshot();
 
+        const auto detected_process_target = process_target;
+        const bool library_applet_target =
+            process_target != applet_bgm::SilentTitleId &&
+            process_target != applet_bgm::QlaunchTitleId &&
+            process_target != applet_bgm::LoadingStateId;
+        const bool previous_library_applet =
+            previous_process_target != applet_bgm::SilentTitleId &&
+            previous_process_target != applet_bgm::QlaunchTitleId &&
+            previous_process_target != applet_bgm::LoadingStateId;
+        if (library_applet_target && !previous_library_applet) {
+            applet_home_scene_baseline = scene_snapshot.update_count;
+            applet_home_override = false;
+        }
+        if (library_applet_target &&
+            scene_snapshot.availability ==
+                qlaunch_scene::SceneAvailability::Ready &&
+            scene_snapshot.scene == applet_bgm::QlaunchSceneHome &&
+            scene_snapshot.update_count != applet_home_scene_baseline) {
+            // Library applet processes may linger after HOME is pressed. A
+            // new Home scene report after that applet became active describes
+            // the visible owner and takes precedence over the stale process.
+            applet_home_override = true;
+        }
+        if (!library_applet_target) {
+            applet_home_override = false;
+        } else if (applet_home_override) {
+            process_target = applet_bgm::QlaunchTitleId;
+        }
+        previous_process_target = detected_process_target;
+
         // Keep the late PDM cursor current even while PSC is healthy. If the
         // coordinated observer ever fails mid-cycle, the fallback can then
         // consume the next real event instead of re-baselining past it and
@@ -1325,6 +1361,22 @@ void PmdmntThreadFunc(void*) {
             ? applet_bgm::SilentTitleId
             : ResolveQlaunchState(process_target, scene_snapshot);
 
+        if (target == applet_bgm::LoadingStateId) {
+            if (!loading_delay_active) {
+                loading_delay_active = true;
+                loading_delay_since_tick = armGetSystemTick();
+            }
+            const auto delay_ns = static_cast<u64>(
+                g_loading_start_delay_ms.load(std::memory_order_acquire)) *
+                1'000'000ULL;
+            if (armTicksToNs(
+                    armGetSystemTick() - loading_delay_since_tick) < delay_ns) {
+                target = applet_bgm::SilentTitleId;
+            }
+        } else {
+            loading_delay_active = false;
+        }
+
         if (!sleeping && !power_audio_hold) {
             const bool scene_ready =
                 scene_snapshot.availability ==
@@ -1795,6 +1847,7 @@ void ReloadOstMisc() {
     g_fade_out_ms = config::get_fade_out_ms();
     g_mid_song_fade_in_ms = config::get_mid_song_fade_in_ms();
     g_mid_song_fade_out_ms = config::get_mid_song_fade_out_ms();
+    g_loading_start_delay_ms = config::get_loading_start_delay_ms();
     g_startup_on_wake = config::get_startup_on_wake();
     g_separate_wake_playlist = config::get_separate_wake_playlist();
     g_album_video_volume = config::get_album_video_volume();
diff --git a/sys-tune/source/ui_activity_observer.cpp b/sys-tune/source/ui_activity_observer.cpp
index f2765e1..a83fd2b 100644
--- a/sys-tune/source/ui_activity_observer.cpp
+++ b/sys-tune/source/ui_activity_observer.cpp
@@ -26,6 +26,8 @@ bool g_application_session_started{};
 bool g_application_backgrounded{};
 bool g_application_internal_out_of_focus{};
 bool g_application_slot_baselined{};
+bool g_overlay_open{};
+bool g_overlay_holds_loading{};
 u32 g_update_count{};
 
 bool ProgramIdsMatch(u64 event_program_id, u64 application_program_id) {
@@ -97,14 +99,28 @@ void ApplyEventLocked(
     g_info.last_program_id = program_id;
     g_info.has_last_event = true;
 
-    if (allow_overlay_events && applet_id == AppletId_OverlayApplet &&
-        program_id == OverlayDispProgramId) {
+    if (allow_overlay_events && program_id == OverlayDispProgramId) {
         ++g_info.overlay_event_count;
         g_info.has_overlay_signal = true;
-        if (event_type == PdmAppletEventType_InFocus) {
-            SetQuickSettingsLocked(true);
+        if (event_type == PdmAppletEventType_Launch ||
+            event_type == PdmAppletEventType_InFocus) {
+            g_overlay_open = true;
+            if (g_info.loading_active) {
+                // overlayDisp owns Nintendo's application-launch animation as
+                // well as hold-HOME Quick Settings. During a launch it is the
+                // visual lifetime signal, not a request to lower the volume.
+                g_overlay_holds_loading = true;
+                SetQuickSettingsLocked(false);
+            } else {
+                SetQuickSettingsLocked(true);
+            }
         } else if (IsOutOfFocusEvent(event_type) || IsExitEvent(event_type)) {
+            g_overlay_open = false;
             SetQuickSettingsLocked(false);
+            if (g_overlay_holds_loading) {
+                g_overlay_holds_loading = false;
+                SetLoadingLocked(false);
+            }
         }
     }
 
@@ -169,6 +185,12 @@ void ApplyEventLocked(
         g_application_internal_out_of_focus = false;
         g_info.application_out_of_focus = false;
         SetLoadingLocked(launch_should_load);
+        if (launch_should_load && g_overlay_open) {
+            // The overlay focus event can precede the PDM application Launch
+            // row. Reclassify it from Quick Settings to launch animation.
+            g_overlay_holds_loading = true;
+            SetQuickSettingsLocked(false);
+        }
     } else if (event_type == PdmAppletEventType_InFocus) {
         if (!matches_current) {
             g_pending_application_loading = false;
@@ -178,7 +200,9 @@ void ApplyEventLocked(
             g_application_backgrounded = false;
             g_application_internal_out_of_focus = false;
             g_info.application_out_of_focus = false;
-            SetLoadingLocked(false);
+            if (!g_overlay_holds_loading) {
+                SetLoadingLocked(false);
+            }
         }
     } else if (event_type == PdmAppletEventType_OutOfFocus) {
         if (!matches_current) {
@@ -276,6 +300,8 @@ void Initialize(bool pdm_available) {
     g_application_backgrounded = false;
     g_application_internal_out_of_focus = false;
     g_application_slot_baselined = false;
+    g_overlay_open = false;
+    g_overlay_holds_loading = false;
     g_update_count = 0;
 }
